# Application variables
PORT=8080

# Database credentials
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=exports_db

# Full connection string (used by app if provided directly)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/exports_db
